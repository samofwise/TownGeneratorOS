import { Polygon } from './polygon';

export class Street extends Polygon {
    // Streets are essentially polygons, but might have additional properties later
    // For now, it extends Polygon directly.
}
